# assets/

Deja aquí `fondo.jpg` si quieres una foto de fondo detrás de la mesa.
El juego la carga sola (`css/styles.css`, regla `.fondo`). Si el archivo no
existe, se ve el degradado de reserva y no pasa nada más.
